
import { assignEmoji } from 'fithub/helpers/assign-emoji';
import { module, test } from 'qunit';

module('Unit | Helper | assign emoji');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = assignEmoji([42]);
  assert.ok(result);
});

