
import { pikadayInput } from 'fithub/helpers/pikaday-input';
import { module, test } from 'qunit';

module('Unit | Helper | pikaday input');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = pikadayInput([42]);
  assert.ok(result);
});

