
import { searchableSelect } from 'fithub/helpers/searchable-select';
import { module, test } from 'qunit';

module('Unit | Helper | searchable select');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = searchableSelect([42]);
  assert.ok(result);
});

